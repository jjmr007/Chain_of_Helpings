pragma solidity ^0.5.12;

contract Mutual {
    
    mapping (address => uint) public sAldos;
    
    function transfer(address a, uint pago) public returns (bool) {
     
     
        
    }
    
    
    
    
}
